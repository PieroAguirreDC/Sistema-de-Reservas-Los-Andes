export declare class PagosService {
    findAll(): never[];
    findOne(id: string): {
        id: string;
    };
    create(data: object): object;
    update(id: string, data: object): {
        id: string;
    };
    remove(id: string): {
        id: string;
    };
}
